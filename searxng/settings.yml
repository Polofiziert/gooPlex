use_default_settings: true

server:
  # Overridden at runtime by SEARXNG_SECRET (docker-compose reads repo-root .env)
  secret_key: "ultrasecretkey"
  limiter: false

search:
  formats:
    - html
    - json
